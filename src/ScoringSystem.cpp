#include "lib/ScoringSystem.h"

// digunakan untuk menghitung score yang didapat setelah input
int ScoringSystem::ComputeScore(int input)
{
    // bisa ditambahi beberapa logika seperti multiplier
    return input;
}